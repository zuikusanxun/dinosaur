package modifier.modifierdome;

/*
 *访问修饰符:修饰 类,接口,成员变量,成员方法
 *
 * public:在任意位置都可以访问(同类,同包,不同包)
 * protected:在任意的位置都可以访问(同类,同包,不同包是需要继承关系的)
 * 默认(缺省):包级别的访问权限
 * private:类级别的访问权限
 *
 *
 * */
public class Dome {

}
